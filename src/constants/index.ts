export const apiUrl = "https://api.spaceflightnewsapi.net/v3";

export const endpoints = {
    articles: `${apiUrl}/articles`,
}

export const filtersByPriority = ["title_contains", "summary_contains"];
